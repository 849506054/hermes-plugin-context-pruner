name: context-pruner
version: "3.0"
description: "Session health maintenance for long sessions. Health gate (180 msg skip) + staleness detection + conservative 20% max pruning. Protects prompt-cache prefix."
author: hermes-agent
